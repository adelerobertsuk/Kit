# Handoff — 2026-08-10 (late session)

## Shipped tonight

**Memory Card v1** — Interceptor template, per roadmap scope:
- Tap any Memory Bank card to open a review sheet (`MemoryCardReviewView.swift`).
- Editable title + summary, committed back to the Memory Bank on save.
- Explicit privacy notice ("stays private until you share it, never includes raw transcript/exact time/location").
- Removable "Made with KITT" signature toggle.
- Save privately (primary) or Share (renders the card to an image via `ImageRenderer`, opens the real iOS Share Sheet with image + plain text).
- Verified end-to-end in Simulator: card renders correctly, Share Sheet opens with rendered card.
- New files: `MemoryCardView.swift`, `MemoryCardReviewView.swift`, `ShareSheet.swift`. `JournalEntry` gained an editable `title` field.

**App icon** — reverted to the original `kit-icon-1024.png` (flatter bars) from the `-v2` concept.

**Persona name** — reverted the in-app spoken/displayed name from "KIT" back to **KITT**: wordmark, voice-box accessibility labels, Gemini system prompts (so KITT refers to itself correctly), transcript labels, Now Playing title, Memory Card wordmark/signature. Kept both "kitt" and "kit" as valid spoken close phrases since speech-to-text will likely still transcribe spoken "KITT" as "kit" either way.

**Not yet done:** the Xcode project/target name, bundle identifier, and scheme are still "KIT" — deliberately left alone tonight to avoid an unverified structural change. That's tomorrow's first job if the KITT rename sticks.

## Known gaps carried over (unchanged tonight)
- On-device speech recognition isn't force-flagged (`requiresOnDeviceRecognition`).
- AirPods can't reliably start turn one (needs "Now Playing" status first).
- `Secrets.swift` (Gemini key) should move to Keychain before any TestFlight build.
- No interruption handling yet (calls, Siri, alarms, backgrounding).
- Everything shipped today only tested in Simulator — no real mic/AirPods pass yet.

## For GG / Gemini review
Worth a look: whether "KITT" (vs "KIT") changes anything about the earlier TTS/STT pronunciation concern that originally drove the KIIT→KIT rename, and whether the Xcode-level rename (bundle ID, project name) should happen before or after a device test.
